[build]
target = "../../avr-specs/avr-atmega128rfa1.json"

[target.'cfg(target_arch = "avr")']
runner = "ravedude quadremote -cb 57600"

[unstable]
build-std = ["core"]
build-std-features = ["compiler-builtins-mangled-names"]
